# Requirements for Weather-Aware Object Detection Dataset Preparation & Training
# Based on paper: "Weather-Aware Object Detection Transformer for Domain Adaptation"

# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
tqdm>=4.62.0
Pillow>=8.3.0
matplotlib>=3.4.0
PyYAML>=6.0

# PDF reading (for reading the paper)
pypdf>=3.0.0

# PyTorch and Deep Learning (Required for training)
torch>=2.0.0
torchvision>=0.15.0
tensorboard>=2.11.0

# RT-DETR Model
# Option 1: Ultralytics (Recommended for paper implementation)
ultralytics>=8.0.0

# Option 2: Hugging Face Transformers (Alternative)
# transformers>=4.30.0
# timm>=0.9.0

# Evaluation metrics
pycocotools>=2.0.6
scipy>=1.7.0

# Data augmentation (Optional but recommended)
albumentations>=1.3.0

# Visualization and logging
seaborn>=0.11.0
wandb  # Optional: For experiment tracking
